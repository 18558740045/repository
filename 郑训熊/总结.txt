学习了用springboot替代原有的ssm框架，通过引入启动器来简化父工程的依赖管理。引入了启动器的概念，作为项目启动的入口。而其他的改变是来自于对于配置文件管理的改变，由application.properties(或yml)承担了包括jdbc连接池的配置、服务器端口号的修改、日志级别的设定以及mabatis的相关设置（驼峰匹配、别名、映射路径等）。对于配置类引入了@configuration注解来注释，以及其他的一些类来完全取代原先ssm中的
xml配置。虽然springboot进一步简化了基于ssm框架的开发，但实质上仍然还是基于ssm框架的，并没有改变其本质，仅仅只是更加简化了。